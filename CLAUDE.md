# CLAUDE.md

Read `AGENTS.md` for role, non-negotiables, and commands; `notes/project-brief.md`
for the full strategic brief; `STATUS.md` for current state and next action.

Claude-specific notes:

- This is a software project (Developer role), not a paper: no LaTeX build,
  but the portfolio source-grounding law still governs any corpus data,
  statistics, or linguistic examples.
- The paper this implements lives at
  `papers/retarget/grammaticality-de-idealized/`. When the paper's math and
  this code disagree, the paper wins unless DECISIONS.md says otherwise.
- Run `make test` after touching `js/engine.js` or `tests/`.
